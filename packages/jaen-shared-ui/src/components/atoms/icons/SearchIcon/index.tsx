import {Icon, IconProps} from '@chakra-ui/react'
import {SearchIcon as _SearchIcon} from '@chakra-ui/icons'

type SearchIconProps = IconProps

const SearchIcon: React.FC<SearchIconProps> = props => {
  return <_SearchIcon />
}

export default SearchIcon
